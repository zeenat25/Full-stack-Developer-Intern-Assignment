import React from 'rendereact';
import ReactDOM from 'react-dom';
import MarkSpam from './MarkSpam';

ReactDOM.render(
  <React.StrictMode>
    <MarkSpam />
  </React.StrictMode>,
  document.getElementById('root')
);